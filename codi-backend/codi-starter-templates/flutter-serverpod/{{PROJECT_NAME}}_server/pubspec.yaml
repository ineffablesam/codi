name: {{PROJECT_NAME}}_server
description: Serverpod backend server for {{PROJECT_NAME}}
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  serverpod: ^2.0.0
  serverpod_auth_server: ^2.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
