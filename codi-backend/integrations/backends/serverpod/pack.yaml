name: serverpod
version: 2.0.0
pack_type: backend
description: "Serverpod is a scalable Dart backend framework with ORM, WebSocket support, and automatic API generation"

compatibility:
  dart: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"
  serverpod: "^2.0.0"

tags:
  - dart
  - backend
  - orm
  - websocket
  - rpc
